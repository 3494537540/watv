# 首页轮播钉住 / 登录空态 / 浅色弹窗

## 1. 往下滑轮播图不动

**需求**：列表下滑时 Banner **海报固定**，白底内容盖上去（不是“能拉动 Banner”）。

**实现**：`home_page.dart` 底层 `HomePinnedBannerLayer` 固定海报；滚动层 `HomeImmersiveHeader(showBackdrop: false)` 只负责左右滑切页与标题浮层。

## 2. 去掉蓝底「去登录」硬按钮

任务页未登录空态改为浅灰卡片 + 圆形图标 + **描边按钮**（`tasks_page.dart` `_EmptyState`）。

## 3. 底部弹窗浅色 + 输入框

求片 / 播放报错底栏改为白底圆角，输入框浅灰底+描边聚焦蓝（`request_vod_sheet.dart`、`play_error_report.dart`）。
